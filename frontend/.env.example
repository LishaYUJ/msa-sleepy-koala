# Local development can leave this empty because Vite proxies /api to the backend.
# Set this to the deployed Azure App Service URL in Vercel.
VITE_API_BASE_URL=https://your-app-name.azurewebsites.net
